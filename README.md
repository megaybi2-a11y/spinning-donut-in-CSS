
# 🍩 Spinning Donut Website

A tiny, fun static website that displays a smoothly spinning donut using pure HTML and CSS.

No JavaScript. No frameworks. Just vibes.

## ✨ Features

- CSS-only spinning donut animation
- Uses `conic-gradient` and masking for the donut shape
- Lightweight and fast
- Perfect for demos, jokes, or minimal landing pages

## 📁 Project Structure

generated this shi with chatgpt who actually writes readme.md
